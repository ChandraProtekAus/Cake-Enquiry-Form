## Requirements
* npm (node package manager)
* Node.js
* Rubygems
* Sass
* Compass


install node modules:
  
  ```
  npm install
  ```
 Run grunt to start the engines
  
  ```
  grunt
  ```
 Navigate to local server
  
  ```
  http://localhost:8000/
  ```
